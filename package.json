{
  "name": "skill-exchange-2.0",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node src/server.js"
  }
}
